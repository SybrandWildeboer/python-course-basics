"""HOMEWORK 2: a menu program.

Print a numbered menu, ask the user to pick an option, call the matching
function, print the result, and keep going until they quit.

    1. Is a number even?
    2. Celsius to Fahrenheit
    3. Initials from a name
    4. Longest word in a sentence
    5. Quit

Requirements:
  * reuse your task 1 functions, do not rewrite them
  * an option that is not on the menu gets a polite message and another go
  * a word typed where a number belongs should not take the program down
    (that last one is hard with what you know today; have a think about it,
    and see the note in the README)

To reuse the functions from the other file, put this at the top:

    from small_functions import is_even, celsius_to_fahrenheit
    from small_functions import initials, longest_word

That only works when both files sit in the same folder and you run the program
from that folder. Imports get a proper explanation next session.
"""


# TODO: the import line

# TODO: a while loop that shows the menu, reads a choice and acts on it
